{
  "name": "wikidata-inat-checker-mcp",
  "version": "1.0.0",
  "description": "Read-only MCP server over wikidata-inat-checker's rotated NDJSON logs. See docs/mcp-server.md.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=26"
  },
  "main": "main.js",
  "scripts": {
    "start": "node main.js",
    "test": "node --test test/*.test.js",
    "lint": "oxlint",
    "typecheck": "tsc -p jsconfig.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "express": "^5.2.1",
    "zod": "^4.5.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/node": "^26.3.0",
    "oxlint": "^1.80.0",
    "typescript": "^7.0.2"
  }
}
