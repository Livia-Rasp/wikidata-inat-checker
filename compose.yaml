# Run the web app against the findings database in ./data:
#
#   docker compose up --build      then open http://localhost:8080
#
# `image:` points at the published tag (see docs/container.md) and `build: .` is local-dev
# convenience only — the *server* must never run `docker compose up --build` itself, since that
# rebuilds from whatever source happens to be checked out there rather than the image CI already
# built and smoke-tested. Backing the database up is tools/backup.mjs (`npm run backup`), run from
# the host on a timer; see docs/container.md. Network exposure for anyone beyond the operator is
# still undecided (slice 9, narrowed 2026-08-26) — this file does not change that.
#
# No top-level `version:` — it has been obsolete since Compose v2 and only earns a warning.

services:
  web:
    build: .
    image: ghcr.io/livia-rasp/wikidata-inat-checker:latest

    # tini as PID 1. Not for the usual reason: the app already signals its own discovery child
    # (server/app.js's onClose hook SIGTERMs it with a 5 s escalation). This is for reaping any
    # grandchild that gets re-parented when something dies unexpectedly, which PID 1 must do and
    # Node will not do for processes it did not spawn itself.
    init: true
    restart: unless-stopped
    labels:
      # Opt-in, not "watch every container on the box" — vue-commons-gallery already runs
      # nicholas-fedor/watchtower on the home server this is meant to share, polling GHCR for any
      # container carrying this label. This repo deliberately does NOT define its own watchtower
      # service: one poller per host, not one per project. If that other repo's watchtower is ever
      # not running on the deploy host, this label alone buys nothing — redeploys need it present.
      - "com.centurylinklabs.watchtower.enable=true"

    # Defaults to 1000:1000, which is the `node` user in the image — and, on a single-user Linux
    # box, usually you. It has to match whoever owns ./data, because a **bind mount keeps the
    # host's ownership**: Docker chowns a named volume on first use and does nothing of the kind
    # here. Get it wrong and the container exits 1 with "unable to open database file", which is at
    # least loud.
    #
    # If `id -u` says something other than 1000:
    #   WINC_UID=$(id -u) WINC_GID=$(id -g) docker compose up
    # That works because the image sets HOME explicitly, so a uid with no passwd entry inside the
    # container still resolves os.homedir() instead of throwing.
    user: "${WINC_UID:-1000}:${WINC_GID:-1000}"

    # Nothing outside the database directory is written at runtime: no route touches the
    # filesystem, and every write endpoint goes through SQL. /tmp is given a tmpfs because SQLite
    # may want somewhere to spill a large sort.
    read_only: true
    tmpfs:
      - /tmp

    # Bound to the host's loopback, so publishing the port does not also publish the app to the
    # LAN. The API is unauthenticated; see docs/threat-model.md.
    ports:
      - "127.0.0.1:8080:8080"

    # The DIRECTORY, never the .db file. A single-file bind mount is pinned to an inode, so a
    # checkpoint restart or a VACUUM INTO would silently detach the container's view from the
    # host's file. WAL also needs to create -wal and -shm alongside the database.
    #
    # A bind mount rather than a named volume so the host's checkers and this server share one
    # file: `npm run images` on the host shows up here with no restart. That is the two-process
    # case lib/db.js was written for (WAL, a busy_timeout, and migrate() under BEGIN IMMEDIATE).
    #
    # WINC_DATA points it somewhere else without editing this file — a VACUUM INTO copy when you
    # want to try something against a throwaway backlog, since `data/findings.db` is the one
    # artifact here that cannot be regenerated:
    #   WINC_DATA=/tmp/scratch/data docker compose up
    volumes:
      - ${WINC_DATA:-./data}:/app/data

      # A bind mount, not a named volume — deliberately, so an external reader (the mcp-logs
      # service added in a later slice) has a plain, directly-readable host path rather than
      # needing `docker volume inspect` first. See docs/logging.md.
      - ${WINC_LOGS:-./logs}:/app/logs

    environment:
      # A container is reached from outside its own network namespace, so the default 127.0.0.1
      # bind would be unreachable through the published port. server/index.js refuses a
      # non-loopback bind unless ALLOW_REMOTE_WRITES says the exposure is deliberate — so these
      # two go together, and neither is optional here.
      HOST: "0.0.0.0"
      ALLOW_REMOTE_WRITES: "1"
      FINDINGS_DB: /app/data/findings.db

      # ALLOWED_HOSTS is deliberately NOT set. The write guard already accepts the loopback
      # names, and the app is reached as localhost:8080, so the Host header is `localhost`.
      # Set it only when a reverse proxy puts a different name in front, together with
      # TRUST_PROXY set to that proxy's address:
      #   ALLOWED_HOSTS: "inat.example"
      #   TRUST_PROXY: "127.0.0.1"

      # LOG_LEVEL and LOG_RETENTION_DAYS are also deliberately NOT set — server/logger.js's
      # defaults (info, 7 days) are fine for this deployment. See docs/threat-model.md.

    # The app's SIGTERM handler drains and exits promptly; this is headroom, not an expectation.
    stop_grace_period: 30s

    # Sized for a discovery run, not for the idle server (which sits around 45 MB).
    #
    # It is tempting to set this low, because discovery cannot be *started from the host* — the
    # privileged routes need a loopback peer and bridge NAT never gives one. That reasoning is a
    # trap: from inside the container the peer really is loopback, so
    # `docker compose exec web node -e "fetch('http://127.0.0.1:8080/api/discover', …)"` starts a
    # run quite happily. Today it dies immediately because the taxa index is not in the image —
    # but the moment anyone mounts that index (a checker service, slice 9), a run would fork a
    # child that materialises 1.4M rows and spikes to ~650 MB. Under a 512m cap the kernel kills
    # it, and `SIGKILL` is deliberately never reported as a cancel, so it surfaces as a mystery.
    #
    # A limit caps, it does not reserve: costing nothing while unused is the whole point.
    mem_limit: 1500m
    memswap_limit: 1500m  # equal to mem_limit disables swap — swapping a WAL database is worse
                          # than failing fast

    # GET / touches no database, and sits outside both the /api rate limiter and the write guard,
    # so probing it cannot exhaust a budget or trip a guard. 127.0.0.1 rather than localhost:
    # Node may resolve localhost to ::1, and the server is listening on IPv4.
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8080/').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
      start_interval: 2s

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  # Read-only MCP server over the logs `web` writes above — see docs/mcp-server.md. Its own
  # container rather than a route inside `web`: a log reader must not be able to affect the app
  # it observes, and the API's own origin must not also grow a tool-calling endpoint.
  mcp-logs:
    build: ./mcp-server
    image: ghcr.io/livia-rasp/wikidata-inat-checker-mcp:latest
    restart: unless-stopped
    labels:
      - "com.centurylinklabs.watchtower.enable=true"

    # Published on the LAN, unlike `web`'s loopback-only port — the whole point of Streamable
    # HTTP here is a shell that may later run on a different machine on the network. The token
    # (MCP_AUTH_TOKEN below) and the Host allowlist stand in for the network-level restriction
    # `web` gets from staying on 127.0.0.1. Port 3400 is registered in the vault's Ports.md.
    ports:
      - "3400:3400"

    volumes:
      # The same bind mount `web` writes into, mounted read-only here — the second, mount-level
      # enforcement of this server's read-only contract; the first is that it registers no write
      # tools at all. See docs/mcp-server.md.
      - ${WINC_LOGS:-./logs}:/logs:ro

    # Copy mcp-server/.env.example to mcp-server/.env first (gitignored) — MCP_AUTH_TOKEN has no
    # default, and this fails to come up without one. See mcp-server/.env.example's own comments.
    env_file:
      - mcp-server/.env

    environment:
      LOG_DIR: /logs

    stop_grace_period: 10s
    mem_limit: 256m
    memswap_limit: 256m
